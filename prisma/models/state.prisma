enum StateType {
    Activo
    Inconcluso
    Disponible
    Suspendido
    Inactivo
    Finalizado
}

model State {
    id   Int       @id @default(autoincrement())
    name StateType @unique

    examenes Examen[]

    @@map("state")
}
